%h2 Ennustused

%table#teams.table.table-striped.table-hover
  %thead
    %tr
      %th Nimi
      %th Tulemus
  %tbody
    - @userTeams.each do |userTeam|
      %tr
        %td= userTeam.user_name
        %td= userTeam.teams
= button_to 'Tagasi', finals_teams_path(@tournament_id, @user.id), :method => :get, :class => 'btn btn-secondary mt-3'